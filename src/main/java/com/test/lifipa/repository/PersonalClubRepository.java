package com.test.lifipa.repository;

import com.test.lifipa.model.PersonalClub;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PersonalClubRepository extends JpaRepository<PersonalClub,Long> {
}
